# Google Sheet 접수 연동

아우데 사이트의 사연 및 상담 신청 화면은 사이트 디자인을 그대로 사용하며, 제출 내용만 비공개 Google Sheet에 저장합니다.

## 1. 스프레드시트 준비

1. 아우데 전용 Google 계정으로 새 스프레드시트를 만듭니다.
2. 주소에서 `/d/`와 `/edit` 사이의 값을 복사합니다. 이것이 `SPREADSHEET_ID`입니다.
3. 스프레드시트의 공유 범위를 **제한됨**으로 두고 다른 편집자를 추가하지 않습니다.

## 2. Apps Script 배포

1. 스프레드시트에서 **확장 프로그램 → Apps Script**를 엽니다.
2. 기본 코드를 지우고 이 저장소의 `google-apps-script/Code.gs`를 붙여넣습니다.
3. **프로젝트 설정 → 스크립트 속성**에 다음 값을 추가합니다.
   - `SPREADSHEET_ID`: 1단계에서 복사한 값
   - `SUBMISSION_SECRET`: 충분히 긴 무작위 문자열
   - `NOTIFY_EMAIL`: 새 접수 알림을 받을 이메일(선택)
4. **배포 → 새 배포 → 웹 앱**을 선택합니다.
   - 다음 사용자로 실행: 나
   - 액세스 권한: 모든 사용자
5. 배포된 웹 앱 URL을 복사합니다.

웹 앱은 공개 호출을 허용하지만, 올바른 비밀값이 포함된 서버 요청만 저장합니다. 비밀값은 브라우저에 노출되지 않습니다.

## 3. Vercel 환경변수

Vercel 프로젝트의 Environment Variables에 다음을 등록합니다.

- `GOOGLE_APPS_SCRIPT_URL`: 배포된 웹 앱 URL
- `SUBMISSION_SECRET`: Apps Script 속성과 동일한 무작위 문자열
- `NEXT_PUBLIC_CONTACT_EMAIL`: 연동 장애 시 사용할 아우데 이메일

등록 후 Production을 재배포합니다.

## 작동 방식

- 연동 전 또는 일시적 장애 시에는 기존 이메일 작성 방식으로 자동 전환됩니다.
- 스크립트가 **접수면접**, **심리검사**, **사연** 탭과 제목 행을 자동으로 만듭니다. 표를 미리 만들 필요가 없습니다.
- 개인·커플·부부 등 상담 신청은 **접수면접** 탭에 저장됩니다.
- 심리검사·해석상담 신청은 **심리검사** 탭에 저장됩니다.
- 익명 사연은 **사연** 탭에 저장됩니다.
- 접수번호는 유형별로 자동 발급됩니다: 접수면접 `I-2026-0001`, 심리검사 `T-2026-0001`, 사연 `S-2026-0001`.
- 상태 기본값은 **신규**이며 시트에서 확인·연락완료·예약확정 등으로 변경할 수 있습니다.
- 이메일 알림에는 민감한 본문을 넣지 않고 새 접수 사실만 안내합니다.

## 운영 전 점검

- Google 계정 2단계 인증
- 스프레드시트 공유 범위 제한
- 개인정보 처리방침의 처리위탁·국외 이전 관련 검토
- 실제 제출 테스트 후 시트 저장과 이메일 알림 확인
- 보유기간에 따른 정기 삭제 절차 마련
